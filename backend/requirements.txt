# SRE-agent 完整依赖 (直接 + 全部传递 = 45 个包)
# 生成: pipdeptree 递归解析 → 去 Windows 专有 → 去条件依赖
# pip install -r requirements.txt 可完整离线安装

# ── Web 框架 ──
fastapi>=0.136
uvicorn[standard]>=0.48
starlette>=1.1
httptools>=0.8
uvloop>=0.22
watchfiles>=1.2
websockets>=16.0

# ── MCP 协议 ──
mcp>=1.27
sse-starlette>=3.4
httpx-sse>=0.4

# ── HTTP 栈 ──
httpx>=0.28
httpcore>=1.0
h11>=0.16
certifi>=2026

# ── 数据库 ──
sqlalchemy>=2.0
aiosqlite>=0.22
greenlet>=3.5

# ── 数据校验 ──
pydantic>=2.13
pydantic-core>=2.46.4
pydantic-settings>=2.14
annotated-types>=0.7
annotated-doc>=0.0.4
typing-extensions>=4.15
typing-inspection>=0.4

# ── 认证 ──
python-multipart>=0.0.29
pyjwt>=2.13

# ── 系统工具 ──
psutil>=7.2
python-dotenv>=1.2
pyyaml>=6.0

# ── 科学计算 (C/Fortran 扩展, LoongArch 需编译) ──
numpy>=2.4
scipy>=1.17
scikit-learn>=1.8
joblib>=1.5
threadpoolctl>=3.6

# ── JSON Schema (MCP 协议必需) ──
jsonschema>=4.26
jsonschema-specifications>=2025.9
referencing>=0.37
rpds-py>=0.30
attrs>=26.1

# ── 异步支持 ──
anyio>=4.13
idna>=3.16
exceptiongroup>=1.3

# ── CLI ──
click>=8.4

# ── 测试 ──
pytest>=9.0
pytest-asyncio>=1.4
pluggy>=1.6
iniconfig>=2.3
packaging>=26
pygments>=2.20